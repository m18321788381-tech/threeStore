[pytest]
asyncio_mode = auto
# pytest-asyncio 未来会默认 function；显式声明以免升级后异步 fixture 的作用域悄悄变掉
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
