# ---- Web ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20

# ---- DB ----
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# ---- Validation / Config ----
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# ---- Auth ----
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# ---- Markdown / Rendering ----
markdown-it-py==3.0.0
# gfm-like 预设会开启 linkify；缺这个包时渲染直接抛 ModuleNotFoundError
linkify-it-py==2.0.3
mdit-py-plugins==0.4.2
pygments==2.18.0
bleach[css]==6.2.0

# ---- Utils ----
python-slugify==8.0.4

# ---- 上传安全：位图真解码校验（防 polyglot 伪装）+ SVG XML 净化（防 XSS）----
pillow==11.0.0
defusedxml==0.7.1

# ---- Redis（可选；worker / SEO 缓存 / 未来限流） ----
redis==5.2.1
